import tensorflow as tf
import numpy as np
"""have 2 text files. Give each word a unique ID
#The problem is that each string has different length every time
#for now we need to have the same length vector for each input

lexicon = [chair, table, spoon, television]

I pulled the chair up to the table

[1, 1, 0, 0]   <-- that's what our vector looks like if we have that sentence
np.zeros(len(lexicon))"""

